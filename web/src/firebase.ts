// Removed for the static, on-device build.
// The Firebase server-verified version is preserved in git history:
// restore this file, re-add the "firebase" dependency, and set VITE_FIREBASE_* env vars.
export {};
