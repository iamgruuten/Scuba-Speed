# The static, on-device build needs NO environment variables.
# MediaPipe assets load from public CDNs; the leaderboard is stored in the browser.
#
# (Only the optional Firebase server-verified mode needs config here. If you wire
# that back up, restore web/src/firebase.ts from git history and set VITE_FIREBASE_* keys.)
